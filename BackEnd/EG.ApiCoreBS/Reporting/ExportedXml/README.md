# Exported XML Reports

Place DevExpress `.repx` files exported from the visual designer in this folder.

The generic report provider loads a matching XML report first. If none exists, it falls back to known code-based reports such as ``.

When a stored-procedure report is edited in the web designer, the saved layout is written here and the viewer uses that `.repx` on the next open.
