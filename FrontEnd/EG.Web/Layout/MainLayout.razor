@inherits LayoutComponentBase
@inject NavigationManager NavigationManager
@inject SucursalStateService SucursalStateService
@inject IJSRuntime JSRuntime

<link href="/css/menu-overlays.css" rel="stylesheet" />
<script src="/js/download.js"></script>
<script src="/js/mudBlazorTheme.js"></script>

<MudThemeProvider @ref="_themeProvider" Theme="@_theme" IsDarkMode="@_isDarkMode" />
<MudDialogProvider />
<MudPopoverProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Elevation="1" Class="app-bar">
        @if (!esPaginaLogin)
        {
            <MudIconButton Icon="@(_drawerOpen? Icons.Material.Filled.MenuOpen : Icons.Material.Filled.Menu)"
                           Color="Color.Inherit"
                           Edge="Edge.Start"
                           OnClick="@((e) => DrawerToggle())" />
        }
        <MudText Typo="Typo.h5" Class="ml-3 app-title">GE.WebApp</MudText>
        <MudSpacer />

        @if (!esPaginaLogin)
        {
            <!-- Información del usuario y sucursal -->
            <div class="d-flex align-center mr-4">
                <MudChip T="string" Color="Color.Primary" 
                         Variant="Variant.Outlined" 
                         Size="Size.Small"
                         Icon="@Icons.Material.Filled.Store"
                         Class="mr-2">
                    @(SucursalStateService.SucursalNombre ?? "Sin sucursal")
                </MudChip>
                
                <MudChip T="string" Color="Color.Default" 
                         Variant="Variant.Text" 
                         Size="Size.Small"
                         Icon="@Icons.Material.Filled.Person">
                    @nombreUsuario
                </MudChip>
            </div>

            <!-- Selector único con las tres opciones -->
            <MudMenu Icon="@Icons.Material.Filled.ColorLens"
                     Color="Color.Inherit"
                     AnchorOrigin="Origin.TopRight"
                     TransformOrigin="Origin.TopRight"
                     Class="theme-menu">
                @foreach (var option in Enum.GetValues<ThemeOption>())
                {
                    <MudMenuItem OnClick="@(() => SelectThemeOption(option))">
                        <div class="d-flex align-center">
                            @if (option == ThemeOption.Claro)
                            {
                                <MudIcon Icon="@Icons.Material.Outlined.LightMode" Class="mr-2" Size="Size.Small" />
                            }
                            else if (option == ThemeOption.Oscuro)
                            {
                                <MudIcon Icon="@Icons.Material.Outlined.DarkMode" Class="mr-2" Size="Size.Small" />
                            }
                            else if (option == ThemeOption.Morena)
                            {
                                <MudIcon Icon="@Icons.Material.Filled.Park" Class="mr-2" Size="Size.Small" Style="color: #8B4513;" />
                            }
                            @option.ToString()
                        </div>
                    </MudMenuItem>
                }
            </MudMenu>

            <MudMenu Icon="@Icons.Material.Filled.MoreVert" Color="Color.Inherit" AnchorOrigin="Origin.TopRight" TransformOrigin="Origin.TopRight">
                <MudMenuItem OnClick="@Logout">Cerrar sesión</MudMenuItem>
            </MudMenu>
        }
    </MudAppBar>

    <MudDrawer @bind-Open="_drawerOpen"
               ClipMode="DrawerClipMode.Always"
               Elevation="2"
               Width="20%"
               Class="@GetDrawerClass()"
               @onmouseenter="@OnDrawerMouseEnter"
               @onmouseleave="@OnDrawerMouseLeave">
        <DynamicMenu />
    </MudDrawer>

    <MudMainContent Class="main-content">
        <div class="content-wrapper">
            @Body
        </div>
    </MudMainContent>
</MudLayout>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>

@code {
    // Enumerador con las tres opciones
    public enum ThemeOption
    {
        Claro,
        Oscuro,
        Morena
    }

    private MudThemeProvider _themeProvider = null!;
    private ThemeOption _selectedThemeOption { get; set; } = ThemeOption.Oscuro;

    private bool _drawerOpen = true;
    private bool _drawerHovered = false;
    private bool _isDarkMode = true;
    private string _selectedTheme = "default";
    private MudTheme _theme = new MudTheme();
    private string nombreUsuario = "Usuario";
    private bool esPaginaLogin => NavigationManager.Uri.Contains("/login", StringComparison.OrdinalIgnoreCase);

    protected override async Task OnInitializedAsync()
    {
        base.OnInitialized();
        // Inicializar con el tema por defecto (Oscuro)
        OnThemeOptionChanged();
        
        // Cargar información del usuario
        await CargarUsuarioAsync();
        
        // Inicializar servicio de sucursal
        await SucursalStateService.InitializeAsync();
        
        // Suscribirse a cambios de sucursal
        SucursalStateService.OnSucursalChanged += OnSucursalChanged;
    }

    private async Task CargarUsuarioAsync()
    {
        try
        {
            var userName = await JSRuntime.InvokeAsync<string>("localStorage.getItem", "userName");
            if (!string.IsNullOrEmpty(userName))
            {
                nombreUsuario = userName;
            }
            else
            {
                var userId = await JSRuntime.InvokeAsync<string>("localStorage.getItem", "userId");
                if (!string.IsNullOrEmpty(userId))
                {
                    nombreUsuario = $"ID: {userId}";
                }
            }
        }
        catch
        {
            // Si hay error, usar valor por defecto
        }
    }

    private void OnSucursalChanged(int sucursalId, string sucursalNombre)
    {
        StateHasChanged();
    }

    private async Task Logout()
    {
        NavigationManager.NavigateTo("/login", forceLoad: true);
        await Task.CompletedTask;
    }

    private async Task SelectThemeOption(ThemeOption option)
    {
        _selectedThemeOption = option;
        OnThemeOptionChanged();
        await InvokeAsync(StateHasChanged);
        await JSRuntime.InvokeVoidAsync("mudBlazorTheme.applyTheme");
    }

    private void OnThemeOptionChanged()
    {
        switch (_selectedThemeOption)
        {
            case ThemeOption.Claro:
                _isDarkMode = false;
                _selectedTheme = "default";
                break;
            case ThemeOption.Oscuro:
                _isDarkMode = true;
                _selectedTheme = "default";
                break;
            case ThemeOption.Morena:
                _isDarkMode = true;
                _selectedTheme = "morena";
                break;
        }

        LoadTheme();
    }

    private void LoadTheme()
    {
        _theme = new MudTheme
        {
            PaletteLight = GetLightPalette(),
            PaletteDark = GetDarkPalette(),
            LayoutProperties = new LayoutProperties
            {
                DefaultBorderRadius = "10px"
            },
            Typography = new Typography
            {
                Default = new DefaultTypography
                {
                    FontFamily = new[] { "Segoe UI", "Roboto", "Arial", "sans-serif" },
                    FontSize = "0.95rem"
                },
                H5 = new H5Typography
                {
                    FontWeight = "700",
                    FontSize = "1.4rem"
                },
                Overline = new OverlineTypography
                {
                    FontSize = "0.8rem",
                    FontWeight = "500"
                },
                Subtitle2 = new Subtitle2Typography
                {
                    FontSize = "0.875rem",
                    FontWeight = "500"
                }
            }
        };
    }

    private PaletteLight GetLightPalette()
    {
        return new PaletteLight
        {
            Primary = "#2563EB",
            PrimaryLighten = "#3B82F6",
            PrimaryDarken = "#1D4ED8",
            Secondary = "#7C3AED",
            SecondaryLighten = "#8B5CF6",
            SecondaryDarken = "#6D28D9",
            Tertiary = "#059669",
            TertiaryLighten = "#10B981",
            TertiaryDarken = "#047857",
            Background = "#F8FAFC",
            Surface = "#FFFFFF",
            AppbarBackground = "#FFFFFF",
            AppbarText = "#0F172A",
            DrawerBackground = "#F1F5F9",
            DrawerText = "#1E293B",
            DrawerIcon = "#2563EB",
            TextPrimary = "#0F172A",
            TextSecondary = "#475569",
            TextDisabled = "#94A3B8",
            ActionDefault = "#2563EB",
            ActionDisabled = "#CBD5E1",
            ActionDisabledBackground = "#F1F5F9",
            Divider = "#E2E8F0",
            LinesDefault = "#E2E8F0",
            TableLines = "#E2E8F0",
            Info = "#0284C7",
            Success = "#059669",
            Warning = "#D97706",
            Error = "#DC2626",
            GrayLight = "#E2E8F0",
            GrayLighter = "#F1F5F9",
            OverlayLight = "#00000040",
            Black = "#000000",
            White = "#FFFFFF",
        };
    }

    private PaletteDark GetDarkPalette()
    {
        if (_selectedTheme == "morena")
        {
            return new PaletteDark
            {
                Primary = "#CD853F",
                PrimaryLighten = "#DEB887",
                PrimaryDarken = "#8B4513",
                Secondary = "#D2691E",
                SecondaryLighten = "#DEB887",
                SecondaryDarken = "#8B4513",
                Tertiary = "#6B8E23",
                Background = "#1A1209",
                Surface = "#2D1B0F",
                AppbarBackground = "#2D1B0F",
                AppbarText = "#F4E4C1",
                DrawerBackground = "#1A1209",
                DrawerText = "#F4E4C1",
                DrawerIcon = "#CD853F",
                TextPrimary = "#F4E4C1",
                TextSecondary = "#DEB887",
                TextDisabled = "#8B7355",
                ActionDefault = "#CD853F",
                ActionDisabled = "#5D3A1A",
                ActionDisabledBackground = "#3D2A1C",
                Divider = "#5D3A1A",
                LinesDefault = "#5D3A1A",
                TableLines = "#5D3A1A",
                Info = "#5F9EA0",
                Success = "#6B8E23",
                Warning = "#D97706",
                Error = "#B22222",
                GrayLight = "#5D3A1A",
                GrayLighter = "#3D2A1C",
                OverlayLight = "#1A120980",
                Black = "#000000",
                White = "#FFFFFF",
            };
        }

        return new PaletteDark
        {
            Primary = "#3B82F6",
            PrimaryLighten = "#60A5FA",
            PrimaryDarken = "#2563EB",
            Secondary = "#8B5CF6",
            SecondaryLighten = "#A78BFA",
            SecondaryDarken = "#7C3AED",
            Tertiary = "#10B981",
            TertiaryLighten = "#34D399",
            TertiaryDarken = "#059669",
            Background = "#0F172A",
            Surface = "#1E293B",
            AppbarBackground = "#1E293B",
            AppbarText = "#F8FAFC",
            DrawerBackground = "#0F172A",
            DrawerText = "#F8FAFC",
            DrawerIcon = "#3B82F6",
            TextPrimary = "#F8FAFC",
            TextSecondary = "#94A3B8",
            TextDisabled = "#64748B",
            ActionDefault = "#3B82F6",
            ActionDisabled = "#475569",
            ActionDisabledBackground = "#1E293B",
            Divider = "#334155",
            LinesDefault = "#334155",
            TableLines = "#334155",
            Info = "#38BDF8",
            Success = "#34D399",
            Warning = "#FBBF24",
            Error = "#F87171",
            GrayLight = "#334155",
            GrayLighter = "#1E293B",
            OverlayLight = "#0F172A80",
            Black = "#000000",
            White = "#FFFFFF",
        };
    }

    private void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }

    private void OnDrawerMouseEnter()
    {
        if (!_drawerOpen)
        {
            _drawerHovered = true;
            StateHasChanged();
        }
    }

    private void OnDrawerMouseLeave()
    {
        if (!_drawerOpen)
        {
            _drawerHovered = false;
            StateHasChanged();
        }
    }

    private string GetDrawerClass()
    {
        var classes = "custom-drawer";
        if (_drawerHovered && !_drawerOpen)
        {
            classes += " drawer-hovered";
        }
        return classes;
    }
}
