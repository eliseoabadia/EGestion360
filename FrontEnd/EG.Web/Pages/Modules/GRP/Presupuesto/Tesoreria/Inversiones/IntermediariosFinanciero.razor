@page "/Presupuesto/Tesoreria/Inversiones/Intermediarios_Financiero"
@page "/Presupuesto/Tesorería/Inversiones/Intermediarios_Financiero"
@using EG.Domain.DTOs.Responses.Tesoreria
@using EG.Web.Pages.Shared
@using EG.Web.Shared
@inherits BaseCrudPage<IntermediarioFinancieroResponse, IntermediarioFinancieroResponse>

<AccessVerification IsInitialized="@IsInitialized" HasAccess="@HasAccess" NavigateToHome="NavigateToHome">
    <MudContainer Class="px-2 d-flex flex-column" MaxWidth="MaxWidth.False" Style="height: 100vh;">
        <PageHeader Title="Intermediarios Financieros"
                    Subtitle="Catalogo de intermediarios para instrumentos de inversion"
                    Icon="@Icons.Material.Filled.Groups"
                    CanCreate="@CanCreate"
                    OnCreate="CreateItem"
                    OnRefresh="() => table.ReloadData()" />
        <GenericDataGrid TItem="IntermediarioFinancieroResponse"
                      @ref="table"
                      Title="Intermediarios financieros"
                      SearchPlaceholder="Buscar por clave, nombre o RFC..."
                      ShowExportButton="@CanExport"
                      ServerDataFunc="LoadServerData"
                      ExportFunc="ExportToExcel"
                      SearchString="@SearchString"
                      SearchStringChanged="(s) => SearchString = s"
                      OnSearch="OnSearch">
            <Columns>
                <TemplateColumn Resizable="false" T="IntermediarioFinancieroResponse" Title="Acciones" Sortable="false" Filterable="false" Hideable="false"><CellTemplate><MudIconButton Icon="@Icons.Material.Filled.Edit" Visible="@CanUpdate" Size="Size.Small" Color="Color.Primary" OnClick="@(() => EditItem(context.Item.PkidIntermediarioFinanciero))" /><MudIconButton Icon="@Icons.Material.Filled.Delete" Visible="@CanDelete" Size="Size.Small" Color="Color.Error" OnClick="@(() => DeleteItem(context.Item.PkidIntermediarioFinanciero))" /></CellTemplate></TemplateColumn>
                <PropertyColumn T="IntermediarioFinancieroResponse" TProperty="int" Property="x => x.PkidIntermediarioFinanciero" Title="ID" Identifier="PkidIntermediarioFinanciero" />
                <PropertyColumn T="IntermediarioFinancieroResponse" TProperty="string" Property="x => x.Clave" Title="Clave" Identifier="Clave" />
                <PropertyColumn T="IntermediarioFinancieroResponse" TProperty="string" Property="x => x.Nombre" Title="Nombre" Identifier="Nombre" />
                <PropertyColumn T="IntermediarioFinancieroResponse" TProperty="string" Property="x => x.RazonSocial" Title="Razón social" Identifier="RazonSocial" />
                <PropertyColumn T="IntermediarioFinancieroResponse" TProperty="string" Property="x => x.Rfc" Title="RFC" Identifier="Rfc" />
            </Columns>
        </GenericDataGrid>
    </MudContainer>
</AccessVerification>

@code {
    private GenericDataGrid<IntermediarioFinancieroResponse> table = null!;
    protected override string ModuleName => "Inversiones";
    protected override string SubModuleName => "Intermediarios_Financiero";
    protected override Type CreateDialogType => typeof(IntermediarioFinancieroDialog);
    protected override Type EditDialogType => typeof(IntermediarioFinancieroDialog);
    protected override Type DeleteDialogType => typeof(DeleteDialog<IntermediarioFinancieroResponse>);
    protected override string GetDefaultSortLabel() => "Nombre";

    protected override IEnumerable<object> MapToExcelData(IEnumerable<IntermediarioFinancieroResponse> items) =>
        items.Select(x => new { x.PkidIntermediarioFinanciero, x.Clave, x.Nombre, x.RazonSocial, x.Rfc});

    protected override async Task ReloadData()
    {
        if (table != null) await table.ReloadData();
    }
}
