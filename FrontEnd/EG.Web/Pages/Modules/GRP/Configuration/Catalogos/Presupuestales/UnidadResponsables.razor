@page "/configuracion/presupuestales/clave-programa/unidad-responsable"
@using EG.Domain.DTOs.Responses.Presupuestales
@using EG.Web.Pages.Shared
@using EG.Web.Shared
@using MudBlazor
@inherits BaseCrudPage<UnidadResponsableResponse, UnidadResponsableResponse>

<AccessVerification IsInitialized="@IsInitialized"
                    HasAccess="@HasAccess"
                    NavigateToHome="NavigateToHome">
    <MudContainer Class="px-2 d-flex flex-column" MaxWidth="MaxWidth.False" Style="height: 100vh;">
        <PageHeader Title="Unidades Responsables"
                    Subtitle="Catálogo de unidades responsables"
                    Icon="@Icons.Material.Filled.Groups"
                    CanCreate="@CanCreate"
                    OnCreate="CreateItem"
                    OnRefresh="() => table.ReloadData()" />

        <GenericDataGrid TItem="UnidadResponsableResponse"
                      @ref="table"
                      Title="Lista de Unidades Responsables"
                      SearchPlaceholder="Buscar por clave o descripción..."
                      ShowExportButton="@CanExport"
                      ServerDataFunc="LoadServerData"
                      ExportFunc="ExportToExcel"
                      SearchString="@SearchString"
                      SearchStringChanged="(s) => SearchString = s"
                      OnSearch="OnSearch">

            <Columns>
                <TemplateColumn Resizable="false" T="UnidadResponsableResponse" Title="Acciones" Sortable="false" Filterable="false" Hideable="false"><CellTemplate><MudTooltip Text="Agregar hijo"><MudIconButton Icon="@Icons.Material.Filled.Add" Color="Color.Error" Size="Size.Small" Visible="@CanCreate" OnClick="@(() => CrearHijoArea(context.Item.PkidUnidadResponsable, context.Item.Descripcion))" /></MudTooltip><MudTooltip Text="Editar"><MudIconButton Icon="@Icons.Material.Filled.Edit" Visible="@CanUpdate" Color="Color.Primary" Size="Size.Small" OnClick="@(() => EditItem(context.Item.PkidUnidadResponsable))" /></MudTooltip><MudTooltip Text="Eliminar"><MudIconButton Icon="@Icons.Material.Filled.Delete" Visible="@CanDelete" Color="Color.Error" Size="Size.Small" OnClick="@(() => DeleteItem(context.Item.PkidUnidadResponsable))" /></MudTooltip></CellTemplate></TemplateColumn>
                <PropertyColumn T="UnidadResponsableResponse" TProperty="int" Property="x => x.PkidUnidadResponsable" Title="ID" Identifier="PkidUnidadResponsable" />
                <PropertyColumn T="UnidadResponsableResponse" TProperty="string" Property="x => x.Clave" Title="Clave" Identifier="Clave" />
                <PropertyColumn T="UnidadResponsableResponse" TProperty="string" Property="x => x.Descripcion" Title="Descripción" Identifier="Descripcion" />
                <TemplateColumn Resizable="true" T="UnidadResponsableResponse" Title="Área Padre" Identifier="AreaPadreNombre"><CellTemplate>@(context.Item.AreaPadreNombre ?? "-")</CellTemplate></TemplateColumn>
            </Columns>
        </GenericDataGrid>
    </MudContainer>
</AccessVerification>

@code {
    private GenericDataGrid<UnidadResponsableResponse> table = null!;

    protected override string ModuleName => "ClavePrograma";
    protected override string SubModuleName => "UnidadResponsable";

    protected override Type CreateDialogType => typeof(UnidadResponsableDialog);
    protected override Type EditDialogType => typeof(UnidadResponsableDialog);
    protected override Type DeleteDialogType => typeof(DeleteDialog<UnidadResponsableResponse>);

    protected override string GetDefaultSortLabel() => "Clave";

    protected override IEnumerable<object> MapToExcelData(IEnumerable<UnidadResponsableResponse> items)
    {
        return items.Select(u => new
        {
            ID = u.PkidUnidadResponsable,
            Clave = u.Clave,
            Descripcion = u.Descripcion,
            AreaPadre = u.AreaPadreNombre ?? "-",
        });
    }

    protected override async Task<string> GetItemNameForDelete(int id)
    {
        var response = await Service.GetByIdAsync(id);
        return response?.Data?.Descripcion ?? "la unidad responsable";
    }

    protected override async Task ReloadData()
    {
        if (table != null)
        {
            await table.ReloadData();
        }
    }

    private async Task CrearHijoArea(int padreId, string nombrePadre)
    {
        try
        {
            if (!CanCreate)
            {
                Snackbar.Add("No tienes permisos para crear", Severity.Warning);
                return;
            }

            var parameters = new DialogParameters
            {
                ["PadreId"] = padreId,
                ["NombrePadre"] = nombrePadre
            };
            var options = new DialogOptions
            {
                CloseButton = true,
                MaxWidth = MaxWidth.Medium,
                FullWidth = true,
                Position = DialogPosition.Center
            };

            var dialog = await DialogService.ShowAsync<UnidadResponsableDialog>(
                "Crear Unidad Responsable", parameters, options);
            var result = await dialog.Result;

            if (!result!.Canceled)
            {
                await ReloadData();
                Snackbar.Add("Unidad Responsable creada exitosamente", Severity.Success);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error al crear: {UserFacingExceptionMessages.ForDisplay(AppLoggerFactory, ex)}", Severity.Error);
        }
    }
}
