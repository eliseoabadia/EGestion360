@page "/configuracion/Patrimonio/Tipo_Adquisicion"
@using EG.Domain.DTOs.Responses.Patrimonio
@using EG.Web.Pages.Shared
@using EG.Web.Shared
@inherits BaseCrudPage<TipoAdquisicionResponse, TipoAdquisicionResponse>

<AccessVerification IsInitialized="@IsInitialized"
                    HasAccess="@HasAccess"
                    NavigateToHome="NavigateToHome">
    <MudContainer Class="px-2 d-flex flex-column" MaxWidth="MaxWidth.False" Style="height: 100vh;">
        <PageHeader Title="Tipos de Adquisición"
                    Subtitle="Catálogo de tipos de adquisición"
                    Icon="@Icons.Material.Filled.ShoppingCart"
                    CanCreate="@CanCreate"
                    OnCreate="CreateItem"
                    OnRefresh="() => table.ReloadData()" />

        <GenericDataGrid TItem="TipoAdquisicionResponse"
                  @ref="table"
                  Title="Lista de Tipos de Adquisición"
                  SearchPlaceholder="Buscar por clave o descripción..."
                  ShowExportButton="@CanExport"
                  ServerDataFunc="LoadServerData"
                  ExportFunc="ExportToExcel"
                  SearchString="@SearchString"
                  SearchStringChanged="(s) => SearchString = s"
                  OnSearch="OnSearch">

            <Columns>
                <TemplateColumn Resizable="false" T="TipoAdquisicionResponse" Title="Acciones" Sortable="false" Filterable="false" Hideable="false"><CellTemplate><MudTooltip Text="Editar"><MudIconButton Icon="@Icons.Material.Filled.Edit" Visible="@CanUpdate" Color="Color.Primary" Size="Size.Small" OnClick="@(() => EditItem(context.Item.PkidTipoAdq))" /></MudTooltip><MudTooltip Text="Eliminar"><MudIconButton Icon="@Icons.Material.Filled.Delete" Visible="@CanDelete" Color="Color.Error" Size="Size.Small" OnClick="@(() => DeleteItem(context.Item.PkidTipoAdq))" /></MudTooltip></CellTemplate></TemplateColumn>
                <PropertyColumn T="TipoAdquisicionResponse" TProperty="int" Property="x => x.PkidTipoAdq" Title="ID" Identifier="PkidTipoAdq" />
                <PropertyColumn T="TipoAdquisicionResponse" TProperty="string" Property="x => x.Clave" Title="Clave" Identifier="Clave" />
                <PropertyColumn T="TipoAdquisicionResponse" TProperty="string" Property="x => x.Descripcion" Title="Descripción" Identifier="Descripcion" />
                <PropertyColumn T="TipoAdquisicionResponse" TProperty="string" Property="x => x.Descripmovto" Title="Descripción Movto" Identifier="Descripmovto" />
            </Columns>
        </GenericDataGrid>
    </MudContainer>
</AccessVerification>

@code {
    private GenericDataGrid<TipoAdquisicionResponse> table = null!;

    protected override string ModuleName => "Patrimonio";
    protected override string SubModuleName => "Tipo_Adquisicion";

    protected override Type CreateDialogType => typeof(TipoAdquisicionDialog);
    protected override Type EditDialogType => typeof(TipoAdquisicionDialog);
    protected override Type DeleteDialogType => typeof(DeleteDialog<TipoAdquisicionResponse>);

    protected override string GetDefaultSortLabel() => "Descripcion";

    protected override IEnumerable<object> MapToExcelData(IEnumerable<TipoAdquisicionResponse> items)
    {
        return items.Select(u => new
        {
            ID = u.PkidTipoAdq,
            Clave = u.Clave,
            Descripcion = u.Descripcion,
            DescripciónMovto = u.Descripmovto,
        });
    }

    protected override async Task<string> GetItemNameForDelete(int id)
    {
        var response = await Service.GetByIdAsync(id);
        return response?.Data?.Descripcion ?? "el tipo de adquisición";
    }

    protected override async Task ReloadData()
    {
        if (table != null)
        {
            await table.ReloadData();
        }
    }
}
