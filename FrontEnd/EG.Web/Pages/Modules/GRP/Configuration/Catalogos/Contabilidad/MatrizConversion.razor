@page "/configuracion/contabilidad/matriz-conversion"
@using EG.Domain.DTOs.Responses.Contabilidad
@using EG.Web.Pages.Shared
@using EG.Web.Shared
@inherits BaseCrudPage<MatrizConversionResponse, MatrizConversionResponse>

<AccessVerification IsInitialized="@IsInitialized"
    HasAccess="@HasAccess"
    NavigateToHome="NavigateToHome">
    <MudContainer Class="px-2 d-flex flex-column" MaxWidth="MaxWidth.False" Style="height: 100%;">
        <PageHeader Title="Matriz de Conversión"
            Subtitle="Relación entre programas, partidas y cuentas contables"
            Icon="@Icons.Material.Filled.SwapHoriz"
            CanCreate="@CanCreate"
            OnCreate="CreateItem"
            OnRefresh="() => table.ReloadData()" />

        <div class="custom-matriz-table">
            <GenericDataGrid TItem="MatrizConversionResponse"
                @ref="table"
                Title="Lista de Matriz de Conversión"
                SearchPlaceholder="Buscar por programa o partida..."
                ShowExportButton="@CanExport"
                ServerDataFunc="LoadServerData"
                ExportFunc="ExportToExcel"
                SearchString="@SearchString"
                SearchStringChanged="(s) => SearchString = s"
                OnSearch="OnSearch"
                OnRowDoubleClick="OpenDetail">

                <Columns>
                    <TemplateColumn Resizable="false" T="MatrizConversionResponse" Title="Acciones" Sortable="false" Filterable="false" Hideable="false"><CellTemplate><MudTooltip Text="Editar"><MudIconButton Icon="@Icons.Material.Filled.Edit" Visible="@CanUpdate" Color="Color.Primary" Size="Size.Small" OnClick="@(() => EditItem(context.Item.PkidMatrizConversion))" /></MudTooltip><MudTooltip Text="Eliminar"><MudIconButton Icon="@Icons.Material.Filled.Delete" Visible="@CanDelete" Color="Color.Error" Size="Size.Small" OnClick="@(() => DeleteItem(context.Item.PkidMatrizConversion))" /></MudTooltip></CellTemplate></TemplateColumn>
                    <TemplateColumn Resizable="true" T="MatrizConversionResponse" Title="ID" Identifier="PkidMatrizConversion"><CellTemplate>@context.Item.PkidMatrizConversion</CellTemplate></TemplateColumn>
                    <TemplateColumn Resizable="true" T="MatrizConversionResponse" Title="Programa" Identifier="ProgramaClave"><CellTemplate>@context.Item.ProgramaClave</CellTemplate></TemplateColumn>
                    <TemplateColumn Resizable="true" T="MatrizConversionResponse" Title="Partida" Identifier="PartidaDescripcion"><CellTemplate>@context.Item.PartidaDescripcion</CellTemplate></TemplateColumn>
                    <TemplateColumn Resizable="true" T="MatrizConversionResponse" Title="TG" Identifier="TipoGastoDescripcion"><CellTemplate>@($"{context.Item.TipoGastoClave} - {context.Item.TipoGastoDescripcion}".Trim(' ', '-'))</CellTemplate></TemplateColumn>
                    <TemplateColumn Resizable="true" T="MatrizConversionResponse" Title="Aprobado" Identifier="CuentaAprobadoNombre"><CellTemplate>@context.Item.CuentaAprobadoNombre</CellTemplate></TemplateColumn>
                    <TemplateColumn Resizable="true" T="MatrizConversionResponse" Title="Por Ejercer" Identifier="CuentaPorEjercerNombre"><CellTemplate>@context.Item.CuentaPorEjercerNombre</CellTemplate></TemplateColumn>
                    <TemplateColumn Resizable="true" T="MatrizConversionResponse" Title="Modificado" Identifier="CuentaModificadoNombre"><CellTemplate>@context.Item.CuentaModificadoNombre</CellTemplate></TemplateColumn>
                    <TemplateColumn Resizable="true" T="MatrizConversionResponse" Title="Comprometido" Identifier="CuentaComprometidoNombre"><CellTemplate>@context.Item.CuentaComprometidoNombre</CellTemplate></TemplateColumn>
                    <TemplateColumn Resizable="true" T="MatrizConversionResponse" Title="Devengado" Identifier="CuentaDevengadoNombre"><CellTemplate>@context.Item.CuentaDevengadoNombre</CellTemplate></TemplateColumn>
                    <TemplateColumn Resizable="true" T="MatrizConversionResponse" Title="Ejercido" Identifier="CuentaEjercidoNombre"><CellTemplate>@context.Item.CuentaEjercidoNombre</CellTemplate></TemplateColumn>
                    <TemplateColumn Resizable="true" T="MatrizConversionResponse" Title="Pagado" Identifier="CuentaPagadoNombre"><CellTemplate>@context.Item.CuentaPagadoNombre</CellTemplate></TemplateColumn>
                    <TemplateColumn Resizable="true" T="MatrizConversionResponse" Title="Gasto" Identifier="CuentaGastoNombre"><CellTemplate>@context.Item.CuentaGastoNombre</CellTemplate></TemplateColumn>
                </Columns>
            </GenericDataGrid>
        </div>
    </MudContainer>
</AccessVerification>

@code {
    private GenericDataGrid<MatrizConversionResponse> table = null!;

    protected override string ModuleName => "Contabilidad";
    protected override string SubModuleName => "Matriz_Conversion";

    protected override Type CreateDialogType => typeof(MatrizConversionDialog);
    protected override Type EditDialogType => typeof(MatrizConversionDialog);
    protected override Type DeleteDialogType => typeof(DeleteDialog<MatrizConversionResponse>);

    protected override string GetDefaultSortLabel() => "ProgramaClave";

    private async Task OpenDetail(MatrizConversionResponse item)
    {
        var parameters = new DialogParameters
        {
            ["Record"] = CinematicRecordFactory.ForMatrizConversion(item)
        };
        var options = new DialogOptions { MaxWidth = MaxWidth.ExtraLarge, FullWidth = true, CloseButton = false };
        var dialog = await DialogService.ShowAsync<CinematicRecordDialog>(string.Empty, parameters, options);
        await dialog.Result;
    }

    protected override IEnumerable<object> MapToExcelData(IEnumerable<MatrizConversionResponse> items)
    {
        return items.Select(u => new
        {
            ID = u.PkidMatrizConversion,
            Programa = u.ProgramaClave,
            Partida = u.PartidaDescripcion,
            TipoGasto = $"{u.TipoGastoClave} - {u.TipoGastoDescripcion}".Trim(' ', '-'),
            CuentaAprobado = u.CuentaAprobadoNombre,
            CuentaPorEjercer = u.CuentaPorEjercerNombre,
            CuentaModificado = u.CuentaModificadoNombre,
            CuentaComprometido = u.CuentaComprometidoNombre,
            CuentaDevengado = u.CuentaDevengadoNombre,
            CuentaEjercido = u.CuentaEjercidoNombre,
            CuentaPagado = u.CuentaPagadoNombre,
            CuentaGasto = u.CuentaGastoNombre,
            FechaCreacion = u.FechaCreacion?.ToString("dd/MM/yyyy HH:mm") ?? string.Empty
        });
    }
}

<style>
    .custom-matriz-table {
        flex: 1 1 auto;
        min-width: 0;
        max-width: 100%;
        overflow: hidden;
    }

    .custom-matriz-table .eg-generic-table,
    .custom-matriz-table .mud-table {
        min-width: 0;
        width: 100%;
        max-width: 100%;
    }

    .custom-matriz-table .mud-table-container {
        min-width: 0 !important;
        width: 100% !important;
        max-width: 100% !important;
        overflow-x: hidden !important;
    }

    .custom-matriz-table table.mud-table-root {
        width: 100% !important;
        max-width: 100% !important;
        table-layout: fixed;
    }

    .custom-matriz-table .mud-table-cell {
        box-sizing: border-box;
        min-width: 0 !important;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
        padding-left: 8px;
        padding-right: 8px;
    }

    .custom-matriz-table .mud-table-sort-label {
        min-width: 0;
    }

    .custom-matriz-table .col-acciones {
        width: 76px;
        text-align: center;
    }

    .custom-matriz-table .col-id {
        width: 54px;
        text-align: center;
    }

    .custom-matriz-table .col-programa {
        width: 7%;
    }

    .custom-matriz-table .col-partida {
        width: 13%;
    }

    .mud-table-row {
        cursor: pointer;
    }
</style>
