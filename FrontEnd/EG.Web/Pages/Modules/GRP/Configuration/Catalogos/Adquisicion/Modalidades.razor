@page "/configuracion/adquisiciones/modalidad"
@using EG.Domain.DTOs.Responses.Adquisicion
@using EG.Web.Pages.Shared
@using EG.Web.Shared
@inherits BaseCrudPage<ModalidadResponse, ModalidadResponse>

<AccessVerification IsInitialized="@IsInitialized"
                    HasAccess="@HasAccess"
                    NavigateToHome="NavigateToHome">
    <MudContainer Class="px-2 d-flex flex-column" MaxWidth="MaxWidth.False" Style="height: 100vh;">
        <PageHeader Title="Modalidades"
                    Subtitle="Catálogo de modalidades de adquisición"
                    Icon="@Icons.Material.Filled.LocalOffer"
                    CanCreate="@CanCreate"
                    OnCreate="CreateItem"
                    OnRefresh="() => table.ReloadData()" />

        <GenericDataGrid TItem="ModalidadResponse"
                      @ref="table"
                      Title="Lista de Modalidades"
                      SearchPlaceholder="Buscar por descripción..."
                      ShowExportButton="@CanExport"
                      ServerDataFunc="LoadServerData"
                      ExportFunc="ExportToExcel"
                      SearchString="@SearchString"
                      SearchStringChanged="(s) => SearchString = s"
                      OnSearch="OnSearch">

            <Columns>
                <TemplateColumn Resizable="false" T="ModalidadResponse" Title="Acciones" Sortable="false" Filterable="false" Hideable="false">
                    <CellTemplate>
                        <MudTooltip Text="Editar"><MudIconButton Icon="@Icons.Material.Filled.Edit" Visible="@CanUpdate" Color="Color.Primary" Size="Size.Small" OnClick="@(() => EditItem(context.Item.PkidModalidad))" /></MudTooltip>
                        <MudTooltip Text="Eliminar"><MudIconButton Icon="@Icons.Material.Filled.Delete" Visible="@CanDelete" Color="Color.Error" Size="Size.Small" OnClick="@(() => DeleteItem(context.Item.PkidModalidad))" /></MudTooltip>
                    </CellTemplate>
                </TemplateColumn>
                <PropertyColumn T="ModalidadResponse" TProperty="int" Property="x => x.PkidModalidad" Title="ID" Identifier="PkidModalidad" />
                <PropertyColumn T="ModalidadResponse" TProperty="string" Property="x => x.Descripcion" Title="Descripción" Identifier="Descripcion" />
            </Columns>
        </GenericDataGrid>
    </MudContainer>
</AccessVerification>

@code {
    private GenericDataGrid<ModalidadResponse> table = null!;

    protected override string ModuleName => "Adquisiciones";
    protected override string SubModuleName => "Modalidad";

    protected override Type CreateDialogType => typeof(ModalidadDialog);
    protected override Type EditDialogType => typeof(ModalidadDialog);
    protected override Type DeleteDialogType => typeof(DeleteDialog<ModalidadResponse>);

    protected override string GetDefaultSortLabel() => "Descripcion";

    protected override IEnumerable<object> MapToExcelData(IEnumerable<ModalidadResponse> items)
    {
        return items.Select(u => new
        {
            ID = u.PkidModalidad,
            Descripcion = u.Descripcion,
        });
    }

    protected override async Task<string> GetItemNameForDelete(int id)
    {
        var response = await Service.GetByIdAsync(id);
        return response?.Data?.Descripcion ?? "la modalidad";
    }

    protected override async Task ReloadData()
    {
        if (table != null)
        {
            await table.ReloadData();
        }
    }
}
