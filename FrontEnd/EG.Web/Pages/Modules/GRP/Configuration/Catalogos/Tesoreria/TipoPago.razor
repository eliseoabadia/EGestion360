@page "/configuracion/tesoreria/Tipo_Pago"
@using EG.Domain.DTOs.Responses.Tesoreria
@using EG.Web.Pages.Shared
@using EG.Web.Shared
@inherits BaseCrudPage<TipoPagoResponse, TipoPagoResponse>
<AccessVerification IsInitialized="@IsInitialized" HasAccess="@HasAccess" NavigateToHome="NavigateToHome">
    <MudContainer Class="px-2 d-flex flex-column" MaxWidth="MaxWidth.False" Style="height: 100vh;">
        <PageHeader Title="Tipos de Pago" Subtitle="Gestión del catálogo de tipos de pago" Icon="@Icons.Material.Filled.Payment"
            CanCreate="@CanCreate" OnCreate="CreateItem" OnRefresh="() => table.ReloadData()" />
        <GenericDataGrid TItem="TipoPagoResponse"
                      @ref="table"
                      Title="Lista de Tipos de Pago"
                      SearchPlaceholder="Buscar por descripción..."
                      ShowExportButton="@CanExport"
                      ServerDataFunc="LoadServerData"
                      ExportFunc="ExportToExcel"
                      SearchString="@SearchString"
                      SearchStringChanged="(s) => SearchString = s"
                      OnSearch="OnSearch">
            <Columns>
                <TemplateColumn Resizable="false" T="TipoPagoResponse" Title="Acciones" Sortable="false" Filterable="false" Hideable="false"><CellTemplate><MudTooltip Text="Editar"><MudIconButton Icon="@Icons.Material.Filled.Edit" Visible="@CanUpdate" Color="Color.Primary" Size="Size.Small" OnClick="@(() => EditItem(context.Item.PkidTipoPago))" /></MudTooltip><MudTooltip Text="Eliminar"><MudIconButton Icon="@Icons.Material.Filled.Delete" Visible="@CanDelete" Color="Color.Error" Size="Size.Small" OnClick="@(() => DeleteItem(context.Item.PkidTipoPago))" /></MudTooltip></CellTemplate></TemplateColumn>
                <PropertyColumn T="TipoPagoResponse" TProperty="int" Property="x => x.PkidTipoPago" Title="ID" Identifier="PkidTipoPago" />
                <PropertyColumn T="TipoPagoResponse" TProperty="string" Property="x => x.Descripcion" Title="Descripción" Identifier="Descripcion" />
            </Columns>
        </GenericDataGrid>
    </MudContainer>
</AccessVerification>
@code {
    private GenericDataGrid<TipoPagoResponse> table = null!;
    protected override string ModuleName => "Tesoreria";
    protected override string SubModuleName => "Tipo_Pago";
    protected override Type CreateDialogType => typeof(TipoPagoDialog);
    protected override Type EditDialogType => typeof(TipoPagoDialog);
    protected override Type DeleteDialogType => typeof(DeleteDialog<TipoPagoResponse>);
    protected override string GetDefaultSortLabel() => "Descripcion";
    protected override IEnumerable<object> MapToExcelData(IEnumerable<TipoPagoResponse> items) =>
        items.Select(u => new { ID = u.PkidTipoPago, Descripcion = u.Descripcion});

    protected override async Task ReloadData()
    {
        if (table != null)
            await table.ReloadData();
    }
}
