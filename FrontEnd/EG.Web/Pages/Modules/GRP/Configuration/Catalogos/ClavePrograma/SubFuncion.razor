@page "/configuracion/presupuestales/clave-programa/subfuncion"
@using EG.Domain.DTOs.Responses.Presupuestales
@using EG.Web.Pages.Shared
@using EG.Web.Shared
@inherits BaseCrudPage<SubFuncionResponse, SubFuncionResponse>

<AccessVerification IsInitialized="@IsInitialized"
    HasAccess="@HasAccess"
    NavigateToHome="NavigateToHome">
    <MudContainer Class="px-2 d-flex flex-column" MaxWidth="MaxWidth.False" Style="height: 100vh;">
        <PageHeader Title="Subfunciones"
                    Subtitle="Catálogo de subfunciones (SF)"
                    Icon="@Icons.Material.Filled.AccountTree"
                    CanCreate="@CanCreate"
                    OnCreate="CreateItem"
                    OnRefresh="() => table.ReloadData()" />

        <GenericTable TItem="SubFuncionResponse"
                      @ref="table"
                      Title="Lista de Subfunciones"
                      SearchPlaceholder="Buscar por clave o descripción..."
                      ShowExportButton="@CanExport"
                      ServerDataFunc="LoadServerData"
                      ExportFunc="ExportToExcel"
                      SearchString="@SearchString"
                      SearchStringChanged="(s) => SearchString = s"
                      OnSearch="OnSearch">

            <Header>
                <MudTh>Acciones</MudTh>
                <MudTh><MudTableSortLabel SortLabel="PkidSf" T="SubFuncionResponse">ID</MudTableSortLabel></MudTh>
                <MudTh><MudTableSortLabel SortLabel="Clave" T="SubFuncionResponse">Clave Función</MudTableSortLabel></MudTh>
                <MudTh><MudTableSortLabel SortLabel="FuncionDescripcion" T="SubFuncionResponse">Función</MudTableSortLabel></MudTh>
                <MudTh><MudTableSortLabel SortLabel="SubFuncionClave" T="SubFuncionResponse">Clave SubFunción</MudTableSortLabel></MudTh>
                <MudTh><MudTableSortLabel SortLabel="SubFuncionDescripcion" T="SubFuncionResponse">Descripción</MudTableSortLabel></MudTh>
            </Header>

            <Row Context="item">
                <MudTd>
                    <MudTooltip Text="Editar">
                        <MudIconButton Icon="@Icons.Material.Filled.Edit"
                                       Visible="@CanUpdate"
                                       Color="Color.Primary"
                                       Size="Size.Small"
                                       OnClick="@(() => EditItem(item.PkidSf))" />
                    </MudTooltip>
                    <MudTooltip Text="Eliminar">
                        <MudIconButton Icon="@Icons.Material.Filled.Delete"
                                       Visible="@CanDelete"
                                       Color="Color.Error"
                                       Size="Size.Small"
                                       OnClick="@(() => DeleteItem(item.PkidSf))" />
                    </MudTooltip>
                </MudTd>
                <MudTd>@item.PkidSf</MudTd>
                <MudTd>@item.FuncionClave</MudTd>
                <MudTd>@item.FuncionDescripcion</MudTd>
                <MudTd>@item.SubFuncionClave</MudTd>
                <MudTd>@item.SubFuncionDescripcion</MudTd>
            </Row>
        </GenericTable>
    </MudContainer>
</AccessVerification>

@code {
    private GenericTable<SubFuncionResponse> table = null!;

    protected override string ModuleName => "ClavePrograma";
    protected override string SubModuleName => "SubFuncion";

    protected override Type CreateDialogType => typeof(SubFuncionDialog);
    protected override Type EditDialogType => typeof(SubFuncionDialog);
    protected override Type DeleteDialogType => typeof(DeleteDialog<SubFuncionResponse>);

    protected override string GetDefaultSortLabel() => "Descripcion";

    protected override IEnumerable<object> MapToExcelData(IEnumerable<SubFuncionResponse> items)
    {
        return items.Select(u => new
        {
            ID = u.PkidSf,
            ClaveFuncion = u.FuncionClave,
            Funcion = u.FuncionDescripcion ?? string.Empty,
            ClaveSubFuncion = u.SubFuncionClave,
            SubFuncion = u.SubFuncionDescripcion ?? string.Empty,
        });
    }

    protected override async Task<string> GetItemNameForDelete(int id)
    {
        var response = await Service.GetByIdAsync(id);
        return response?.Data?.SubFuncionDescripcion ?? "la subfunción";
    }

    protected override async Task ReloadData()
    {
        if (table != null)
        {
            await table.ReloadData();
        }
    }
}
