@page "/configuracion/presupuestales/clave-programa/funcion"
@using EG.Domain.DTOs.Responses.Presupuestales
@using EG.Web.Pages.Shared
@using EG.Web.Shared
@inherits BaseCrudPage<FnResponse, FnResponse>

<AccessVerification IsInitialized="@IsInitialized"
                    HasAccess="@HasAccess"
                    NavigateToHome="NavigateToHome">
    <MudContainer Class="px-2 d-flex flex-column" MaxWidth="MaxWidth.False" Style="height: 100vh;">
        <PageHeader Title="Funciones"
                    Subtitle="Catálogo de funciones (FN)"
                    Icon="@Icons.Material.Filled.Business"
                    CanCreate="@CanCreate"
                    OnCreate="CreateItem"
                    OnRefresh="() => table.ReloadData()" />

        <GenericDataGrid TItem="FnResponse"
                      @ref="table"
                      Title="Lista de Funciones"
                      SearchPlaceholder="Buscar por clave, descripción o grupo funcional..."
                      ShowExportButton="@CanExport"
                      ServerDataFunc="LoadServerData"
                      ExportFunc="ExportToExcel"
                      SearchString="@SearchString"
                      SearchStringChanged="(s) => SearchString = s"
                      OnSearch="OnSearch">

            <Columns><TemplateColumn T="FnResponse" Title="Acciones" Sortable="false" Filterable="false" Hideable="false"><CellTemplate><MudTooltip Text="Editar"><MudIconButton Icon="@Icons.Material.Filled.Edit" Visible="@CanUpdate" Color="Color.Primary" Size="Size.Small" OnClick="@(() => EditItem(context.Item.PkidFn))" /></MudTooltip><MudTooltip Text="Eliminar"><MudIconButton Icon="@Icons.Material.Filled.Delete" Visible="@CanDelete" Color="Color.Error" Size="Size.Small" OnClick="@(() => DeleteItem(context.Item.PkidFn))" /></MudTooltip></CellTemplate></TemplateColumn><PropertyColumn T="FnResponse" TProperty="int" Property="x => x.PkidFn" Title="ID" Identifier="PkidFn" /><PropertyColumn T="FnResponse" TProperty="string" Property="x => x.Clave" Title="Clave FN" Identifier="Clave" /><PropertyColumn T="FnResponse" TProperty="string" Property="x => x.Descripcion" Title="Función" Identifier="Descripcion" /><PropertyColumn T="FnResponse" TProperty="string" Property="x => x.GfClaveNombre" Title="Grupo Funcional" Identifier="GfClaveNombre" /></Columns>
        </GenericDataGrid>
    </MudContainer>
</AccessVerification>

@code {
    private GenericDataGrid<FnResponse> table = null!;

    protected override string ModuleName => "ClavePrograma";
    protected override string SubModuleName => "Funcion";

    protected override Type CreateDialogType => typeof(FuncionDialog);
    protected override Type EditDialogType => typeof(FuncionDialog);
    protected override Type DeleteDialogType => typeof(DeleteDialog<FnResponse>);

    protected override string GetDefaultSortLabel() => "Descripcion";

    protected override IEnumerable<object> MapToExcelData(IEnumerable<FnResponse> items)
    {
        return items.Select(u => new
        {
            ID = u.PkidFn,
            ClaveGF = u.GfClave,
            GrupoFuncional = u.GfDescripcion ?? string.Empty,
            ClaveFN = u.Clave,
            Funcion = u.Descripcion ?? string.Empty,
            FechaCreacion = u.FechaCreacion?.ToString("dd/MM/yyyy HH:mm") ?? string.Empty,
            UsuarioCreacion = u.UsuarioCreacion
        });
    }

    protected override async Task<string> GetItemNameForDelete(int id)
    {
        var response = await Service.GetByIdAsync(id);
        return response?.Data?.Descripcion ?? "la función";
    }

    protected override async Task ReloadData()
    {
        if (table != null)
        {
            await table.ReloadData();
        }
    }
}
