@page "/Adquisiciones/SolicitudSuficienciaDetalle"
@using System.Globalization
@using EG.Domain.DTOs.Responses.Adquisicion
@using EG.Web.Pages.Shared
@using EG.Web.Shared
@using MudBlazor
@inherits BaseCrudPage<SolicitudSuficienciaDetalleResponse, SolicitudSuficienciaDetalleResponse>

<AccessVerification IsInitialized="@IsInitialized"
                    HasAccess="@HasAccess"
                    NavigateToHome="NavigateToHome">
    <MudContainer Class="px-3 d-flex flex-column" MaxWidth="MaxWidth.False" Style="height: 100vh; overflow: hidden;">
        <PageHeader Title="Detalle Solicitud Suficiencia"
                    Subtitle="Calendario mensual por bien y partida"
                    Icon="@Icons.Material.Filled.FormatListBulleted"
                    ShowCreateButton="false"
                    CanCreate="@CanCreate"
                    OnCreate="CreateItem"
                    OnRefresh="() => table.ReloadData()" />

        <div Style="flex-grow: 1; overflow: hidden; min-height: 0;">
            <GenericDataGrid TItem="SolicitudSuficienciaDetalleResponse"
                          @ref="table"
                          Title="Detalles de solicitud"
                          SearchPlaceholder="Buscar requisicion, partida o bien..."
                          ShowExportButton="@CanExport"
                          ServerDataFunc="LoadServerData"
                          ExportFunc="ExportToExcel"
                          SearchString="@SearchString"
                          SearchStringChanged="(s) => SearchString = s"
                          OnSearch="OnSearch">
            <Columns>
                <TemplateColumn T="SolicitudSuficienciaDetalleResponse" Title="Acciones" Identifier="__actions" Sortable="false" Filterable="false" Hideable="false" Resizable="false">
                    <CellTemplate Context="cell">
                        <MudTooltip Text="Editar calendario">
                        <MudIconButton Icon="@Icons.Material.Filled.EditCalendar"
                        Visible="@(CanUpdate && cell.Item.SolicitudEstatus == 1)"
                        Color="Color.Primary"
                        Size="Size.Small"
                        OnClick="@(() => EditItem(cell.Item.PkidSolicitudSuficienciaDetalle))" />
                        </MudTooltip>
                        <MudTooltip Text="Eliminar">
                        <MudIconButton Icon="@Icons.Material.Filled.Delete"
                        Visible="@(CanDelete && cell.Item.SolicitudEstatus == 1)"
                        Color="Color.Error"
                        Size="Size.Small"
                        OnClick="@(() => DeleteItem(cell.Item.PkidSolicitudSuficienciaDetalle))" />
                        </MudTooltip>
                    </CellTemplate>
                </TemplateColumn>
                <TemplateColumn T="SolicitudSuficienciaDetalleResponse" Title="ID" Identifier="PkidSolicitudSuficienciaDetalle" PropertyName="PkidSolicitudSuficienciaDetalle" SortBy="x => x.PkidSolicitudSuficienciaDetalle">
                    <CellTemplate Context="cell">
                        @cell.Item.PkidSolicitudSuficienciaDetalle
                    </CellTemplate>
                </TemplateColumn>
                <TemplateColumn T="SolicitudSuficienciaDetalleResponse" Title="Requisicion" Identifier="RequisicionDescripcion" PropertyName="RequisicionDescripcion" SortBy="x => x.RequisicionDescripcion">
                    <CellTemplate Context="cell">
                        @TextOrDash(cell.Item.RequisicionDescripcion)
                    </CellTemplate>
                </TemplateColumn>
                <TemplateColumn T="SolicitudSuficienciaDetalleResponse" Title="Partida" Identifier="PartidaClave" PropertyName="PartidaClave" SortBy="x => x.PartidaClave">
                    <CellTemplate Context="cell">
                        @TextOrDash(cell.Item.PartidaClaveNombre)
                    </CellTemplate>
                </TemplateColumn>
                <TemplateColumn T="SolicitudSuficienciaDetalleResponse" Title="Bien" Identifier="TipoBienDescripcion" PropertyName="TipoBienDescripcion" SortBy="x => x.TipoBienDescripcion">
                    <CellTemplate Context="cell">
                        <MudText Typo="Typo.body2" Style="font-weight: 600;">@TextOrDash(cell.Item.TipoBienDescripcion)</MudText>
                        <MudText Typo="Typo.caption" Color="Color.Secondary">@TextOrDash(cell.Item.Observaciones)</MudText>
                    </CellTemplate>
                </TemplateColumn>
                <TemplateColumn T="SolicitudSuficienciaDetalleResponse" Title="Cantidad" Identifier="Cantidad" PropertyName="Cantidad" SortBy="x => x.Cantidad">
                    <CellTemplate Context="cell">
                        @Quantity(cell.Item.Cantidad)
                    </CellTemplate>
                </TemplateColumn>
                <TemplateColumn T="SolicitudSuficienciaDetalleResponse" Title="Total" Identifier="Total" PropertyName="Total" SortBy="x => x.Total">
                    <CellTemplate Context="cell">
                        <strong>@Money(DetalleTotal(cell.Item))</strong>
                    </CellTemplate>
                </TemplateColumn>
            </Columns>
        </GenericDataGrid>
        </div>
    </MudContainer>
</AccessVerification>

@code {
    private GenericDataGrid<SolicitudSuficienciaDetalleResponse> table = null!;

    protected override string ModuleName => "Adquisiciones";
    protected override string SubModuleName => "SolicitudSuficiencia";

    protected override Type CreateDialogType => typeof(SolicitudSuficienciaDetalleDialog);
    protected override Type EditDialogType => typeof(SolicitudSuficienciaDetalleDialog);
    protected override Type DeleteDialogType => typeof(DeleteDialog<SolicitudSuficienciaDetalleResponse>);

    protected override string GetDefaultSortLabel() => "PkidSolicitudSuficienciaDetalle";

    private static readonly CultureInfo NumberCulture = CultureInfo.InvariantCulture;

    protected override IEnumerable<object> MapToExcelData(IEnumerable<SolicitudSuficienciaDetalleResponse> items)
    {
        return items.Select(item => new
        {
            ID = item.PkidSolicitudSuficienciaDetalle,
            Requisicion = item.RequisicionDescripcion,
            Partida = item.PartidaClaveNombre,
            Bien = item.TipoBienDescripcion,
            item.Cantidad,
            item.Enero,
            item.Febrero,
            item.Marzo,
            item.Abril,
            item.Mayo,
            item.Junio,
            item.Julio,
            item.Agosto,
            item.Septiembre,
            item.Octubre,
            item.Noviembre,
            item.Diciembre,
            Total = DetalleTotal(item),
            item.Observaciones,
            item.UsuarioCreacion,
            FechaCreacion = item.FechaCreacion?.ToString("dd/MM/yyyy HH:mm") ?? string.Empty,
            item.UsuarioModificacion,
            FechaModificacion = item.FechaModificacion?.ToString("dd/MM/yyyy HH:mm") ?? string.Empty
        });
    }

    protected override async Task<string> GetItemNameForDelete(int id)
    {
        var response = await Service.GetByIdAsync(id);
        return response?.Data?.TipoBienDescripcion ?? "el detalle de solicitud";
    }

    protected override async Task ReloadData()
    {
        if (table != null)
        {
            await table.ReloadData();
        }
    }

    private static string TextOrDash(string? value) =>
        string.IsNullOrWhiteSpace(value) ? "-" : value;

    private static string Quantity(decimal? value) =>
        (value ?? 0m).ToString("0.00", NumberCulture);

    private static string Money(decimal value) =>
        $"${value.ToString("0.00", NumberCulture)}";

    private static decimal DetalleTotal(SolicitudSuficienciaDetalleResponse item) =>
        item.Total ?? item.Enero.GetValueOrDefault() + item.Febrero.GetValueOrDefault() +
        item.Marzo.GetValueOrDefault() + item.Abril.GetValueOrDefault() +
        item.Mayo.GetValueOrDefault() + item.Junio.GetValueOrDefault() +
        item.Julio.GetValueOrDefault() + item.Agosto.GetValueOrDefault() +
        item.Septiembre.GetValueOrDefault() + item.Octubre.GetValueOrDefault() +
        item.Noviembre.GetValueOrDefault() + item.Diciembre.GetValueOrDefault();
}
