@using MudBlazor

@inject NavigationManager NavigationManager

<MudPaper Elevation="1" Class="pa-2 mb-3 pbr-top-navigation">
    <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="1" Class="pbr-top-navigation-stack">
        @if (ShowBackButton)
        {
            <MudTooltip Text="Regresar">
                <MudIconButton Icon="@Icons.Material.Filled.ArrowBack"
                               Color="Color.Primary"
                               Size="Size.Small"
                               OnClick="GoBack" />
            </MudTooltip>
            <MudDivider Vertical="true" FlexItem="true" Class="mx-1" />
        }

        <MudButton Variant="@ButtonVariant("/PBR")"
                   Color="Color.Primary"
                   Size="Size.Small"
                   StartIcon="@Icons.Material.Filled.InsertChart"
                   OnClick="@(() => GoTo("/PBR"))">
            Tablero
        </MudButton>
        <MudButton Variant="@ButtonVariant("/PBR/MiTablero")"
                   Color="Color.Primary"
                   Size="Size.Small"
                   StartIcon="@Icons.Material.Filled.DashboardCustomize"
                   OnClick="@(() => GoTo("/PBR/MiTablero"))">
            Mi Tablero
        </MudButton>
        <MudButton Variant="@ButtonVariant("/PBR/MapaProcesos")"
                   Color="Color.Primary"
                   Size="Size.Small"
                   StartIcon="@Icons.Material.Filled.Route"
                   OnClick="@(() => GoTo("/PBR/MapaProcesos"))">
            Mapa PBR
        </MudButton>
        <MudButton Variant="@ButtonVariant("/PBR/Anteproyectos")"
                   Color="Color.Primary"
                   Size="Size.Small"
                   StartIcon="@Icons.Material.Filled.AssignmentTurnedIn"
                   OnClick="@(() => GoTo("/PBR/Anteproyectos"))">
            Anteproyectos
        </MudButton>
        <MudButton Variant="@ButtonVariant("/PBR/Presupuestos")"
                   Color="Color.Primary"
                   Size="Size.Small"
                   StartIcon="@Icons.Material.Filled.AccountBalanceWallet"
                   OnClick="@(() => GoTo("/PBR/Presupuestos"))">
            Presupuestos
        </MudButton>
        <MudButton Variant="@ButtonVariant("/PBR/MatrizIndicadores")"
                   Color="Color.Primary"
                   Size="Size.Small"
                   StartIcon="@Icons.Material.Filled.AccountTree"
                   OnClick="@(() => GoTo("/PBR/MatrizIndicadores"))">
            Matriz MIR
        </MudButton>
    </MudStack>
</MudPaper>

@code {
    [Parameter] public string BackHref { get; set; } = "/PBR";
    [Parameter] public EventCallback OnBack { get; set; }

    private string CurrentPath
    {
        get
        {
            var path = new Uri(NavigationManager.Uri).AbsolutePath.TrimEnd('/');
            return string.IsNullOrWhiteSpace(path) ? "/" : path;
        }
    }

    private bool ShowBackButton
        => !string.Equals(CurrentPath, "/PBR", StringComparison.OrdinalIgnoreCase);

    private Variant ButtonVariant(string route)
    {
        var cleanRoute = route.TrimEnd('/');
        return string.Equals(CurrentPath, cleanRoute, StringComparison.OrdinalIgnoreCase)
            ? Variant.Filled
            : Variant.Outlined;
    }

    private async Task GoBack()
    {
        if (OnBack.HasDelegate)
        {
            await OnBack.InvokeAsync();
            return;
        }

        NavigationManager.NavigateTo(BackHref);
    }

    private async Task GoTo(string route)
    {
        var cleanRoute = route.TrimEnd('/');
        if (OnBack.HasDelegate && string.Equals(CurrentPath, cleanRoute, StringComparison.OrdinalIgnoreCase))
        {
            await OnBack.InvokeAsync();
            return;
        }

        NavigationManager.NavigateTo(route);
    }
}

<style>
    .pbr-top-navigation {
        border-radius: 8px;
        position: sticky;
        top: 0;
        z-index: 3;
    }

    .pbr-top-navigation-stack {
        overflow-x: auto;
        overflow-y: hidden;
        white-space: nowrap;
        padding-bottom: 2px;
    }

    .pbr-top-navigation-stack .mud-button {
        flex: 0 0 auto;
        min-height: 34px;
    }
</style>
