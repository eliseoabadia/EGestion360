@page "/configuracion/tesoreria/Tipo_PagoSF"
@using EG.Domain.DTOs.Responses.Tesoreria
@using EG.Web.Pages.Shared
@using EG.Web.Shared
@inherits BaseCrudPage<TipoPagoSFResponse, TipoPagoSFResponse>
<AccessVerification IsInitialized="@IsInitialized" HasAccess="@HasAccess" NavigateToHome="NavigateToHome">
    <MudContainer Class="px-2 d-flex flex-column" MaxWidth="MaxWidth.False" Style="height: 100vh;">
        <PageHeader Title="Tipos de Pago SF" Subtitle="Gestión del catálogo de tipos de pago SF" Icon="@Icons.Material.Filled.Receipt"
            CanCreate="@CanCreate" OnCreate="CreateItem" OnRefresh="() => table.ReloadData()" />
        <GenericTable TItem="TipoPagoSFResponse"
                      @ref="table"
                      Title="Lista de Tipos de Pago SF"
                      SearchPlaceholder="Buscar por descripción..."
                      ShowExportButton="@CanExport"
                      ServerDataFunc="LoadServerData"
                      ExportFunc="ExportToExcel"
                      SearchString="@SearchString"
                      SearchStringChanged="(s) => SearchString = s"
                      OnSearch="OnSearch">
            <Header>
                <MudTh>Acciones</MudTh>
                <MudTh><MudTableSortLabel SortLabel="PkidTipoPagoSf" T="TipoPagoSFResponse">ID</MudTableSortLabel></MudTh>
                <MudTh><MudTableSortLabel SortLabel="Descripcion" T="TipoPagoSFResponse">Descripción</MudTableSortLabel></MudTh>
                <MudTh><MudTableSortLabel SortLabel="Activo" T="TipoPagoSFResponse">Activo</MudTableSortLabel></MudTh>
            </Header>
            <Row Context="item">
                <MudTd>
                    <MudTooltip Text="Editar">
                        <MudIconButton Icon="@Icons.Material.Filled.Edit"
                                       Visible="@CanUpdate"
                                       Color="Color.Primary"
                                       Size="Size.Small"
                                       OnClick="@(() => EditItem(item.PkidTipoPagoSf))" />
                    </MudTooltip>
                    <MudTooltip Text="Eliminar">
                        <MudIconButton Icon="@Icons.Material.Filled.Delete"
                                       Visible="@CanDelete"
                                       Color="Color.Error"
                                       Size="Size.Small"
                                       OnClick="@(() => DeleteItem(item.PkidTipoPagoSf))" />
                    </MudTooltip>
                </MudTd>
                <MudTd>@item.PkidTipoPagoSf</MudTd>
                <MudTd>@item.Descripcion</MudTd>
                <MudTd>@item.Activo</MudTd>
            </Row>
        </GenericTable>
    </MudContainer>
</AccessVerification>
@code {
    private GenericTable<TipoPagoSFResponse> table = null!;
    protected override string ModuleName => "Tesoreria";
    protected override string SubModuleName => "Tipo_PagoSF";
    protected override Type CreateDialogType => typeof(TipoPagoSFDialog);
    protected override Type EditDialogType => typeof(TipoPagoSFDialog);
    protected override Type DeleteDialogType => typeof(DeleteDialog<TipoPagoSFResponse>);
    protected override string GetDefaultSortLabel() => "Descripcion";
    protected override IEnumerable<object> MapToExcelData(IEnumerable<TipoPagoSFResponse> items) =>
        items.Select(u => new { ID = u.PkidTipoPagoSf, Descripcion = u.Descripcion, Activo = u.Activo ? "Sí" : "No" });

    protected override async Task ReloadData()
    {
        if (table != null)
            await table.ReloadData();
    }
}
