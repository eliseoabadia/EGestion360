@page "/ayuda"
@page "/configuracion/presupuestales/programa-presupuestal"
@page "/Presupuesto/Tesoreria/Cuentas_Cobrar/Otros_Ingresos"
@page "/Presupuesto/Tesoreria/Planeacion_Gastos"
@page "/Presupuesto/Tesoreria/Solicitud_Reintegros"
@page "/Presupuesto/Tesoreria/Autorizar_Solicitud_Reingresos"
@page "/Presupuesto/Tesoreria/Inversiones/Simulador"
@page "/Presupuesto/Tesoreria/Cuentas_Cobrar/Reportes_CxC/Analisis_Saldos"
@page "/Presupuesto/Tesoreria/Cuentas_Cobrar/Reportes_CxC/Antiguedad_Saldos"
@page "/Presupuesto/Tesoreria/Cuentas_Cobrar/Reportes_CxC/Consulta_Documentos"
@page "/Presupuesto/Tesoreria/Cuentas_Cobrar/Reportes_CxC/Estado_Cuenta"
@page "/Presupuesto/Tesoreria/Cuentas_Cobrar/Reportes_CxC/Integracion_Saldos"
@inject NavigationManager NavigationManager

<MudContainer MaxWidth="MaxWidth.Large" Class="py-6">
    <PageHeader Title="@CurrentItem.Title"
                Subtitle="@CurrentItem.Subtitle"
                Icon="@Icons.Material.Filled.PendingActions"
                ShowCreateButton="false"
                OnRefresh="Refresh" />

    <MudPaper Class="pa-6 mt-4" Elevation="1">
        <MudStack Spacing="3">
            <MudAlert Severity="@CurrentItem.Severity" Variant="Variant.Outlined">
                @CurrentItem.Message
            </MudAlert>

            <MudSimpleTable Dense="true" Hover="true">
                <tbody>
                    <tr>
                        <td><strong>Ruta</strong></td>
                        <td>@CurrentPath</td>
                    </tr>
                    <tr>
                        <td><strong>Modulo</strong></td>
                        <td>@CurrentItem.Module</td>
                    </tr>
                    <tr>
                        <td><strong>Estado</strong></td>
                        <td>@CurrentItem.Status</td>
                    </tr>
                </tbody>
            </MudSimpleTable>
        </MudStack>
    </MudPaper>
</MudContainer>

@code {
    private static readonly IReadOnlyDictionary<string, PendingMenuItem> ItemsByPath =
        new Dictionary<string, PendingMenuItem>(StringComparer.OrdinalIgnoreCase)
        {
            ["/ayuda"] = new(
                "Ayuda",
                "Modulo de soporte",
                "Plataforma",
                "Pendiente",
                "La ruta ya esta registrada para evitar pagina no encontrada; falta definir contenido funcional de ayuda.",
                Severity.Info),
            ["/configuracion/presupuestales/programa-presupuestal"] = new(
                "Clave del Programa",
                "Contenedor de catalogos presupuestales",
                "Presupuesto",
                "Contenedor",
                "Este menu funciona como agrupador; usa sus opciones hijas para operar el catalogo correspondiente.",
                Severity.Info),
            ["/Presupuesto/Tesoreria/Cuentas_Cobrar/Otros_Ingresos"] = PendingCuentasPorCobrar("Otros Ingresos"),
            ["/Presupuesto/Tesoreria/Planeacion_Gastos"] = PendingTesoreria("Planeacion de Gastos"),
            ["/Presupuesto/Tesoreria/Solicitud_Reintegros"] = PendingTesoreria("Solicitud de Reintegros"),
            ["/Presupuesto/Tesoreria/Autorizar_Solicitud_Reingresos"] = PendingTesoreria("Autorizar Solicitud de Reingresos"),
            ["/Presupuesto/Tesoreria/Inversiones/Simulador"] = PendingInversiones("Simulador"),
            ["/Presupuesto/Tesoreria/Cuentas_Cobrar/Reportes_CxC/Analisis_Saldos"] = PendingCuentasPorCobrar("Analisis de Saldos"),
            ["/Presupuesto/Tesoreria/Cuentas_Cobrar/Reportes_CxC/Antiguedad_Saldos"] = PendingCuentasPorCobrar("Antiguedad de Saldos"),
            ["/Presupuesto/Tesoreria/Cuentas_Cobrar/Reportes_CxC/Consulta_Documentos"] = PendingCuentasPorCobrar("Consulta de Documentos"),
            ["/Presupuesto/Tesoreria/Cuentas_Cobrar/Reportes_CxC/Estado_Cuenta"] = PendingCuentasPorCobrar("Estado de Cuenta"),
            ["/Presupuesto/Tesoreria/Cuentas_Cobrar/Reportes_CxC/Integracion_Saldos"] = PendingCuentasPorCobrar("Integracion de Saldos")
        };

    private static PendingMenuItem PendingCuentasPorCobrar(string title) =>
        new(
            title,
            "Operacion de cuentas por cobrar",
            "Presupuesto / Tesoreria",
            "Pendiente de migracion",
            "La opcion ya es navegable, pero su vista operativa aun no esta implementada.",
            Severity.Warning);

    private static PendingMenuItem PendingPlaneacion(string title) =>
        new(
            title,
            "Catalogo de planeacion",
            "Presupuesto / Egreso",
            "Pendiente de migracion",
            "La opcion ya es navegable, pero su vista operativa aun no esta implementada.",
            Severity.Warning);

    private static PendingMenuItem PendingTesoreria(string title) =>
        new(
            title,
            "Operacion de tesoreria",
            "Presupuesto / Tesoreria",
            "Pendiente de migracion",
            "La opcion ya es navegable, pero su vista operativa aun no esta implementada.",
            Severity.Warning);

    private static PendingMenuItem PendingInversiones(string title) =>
        new(
            title,
            "Operacion de inversiones",
            "Presupuesto / Tesoreria",
            "Pendiente de migracion",
            "La opcion ya es navegable, pero su vista operativa aun no esta implementada.",
            Severity.Warning);

    private string CurrentPath => "/" + NavigationManager.ToBaseRelativePath(NavigationManager.Uri).Split('?', '#')[0].Trim('/');

    private PendingMenuItem CurrentItem =>
        ItemsByPath.TryGetValue(CurrentPath, out var item)
            ? item
            : new PendingMenuItem("Menu", "Ruta registrada", "Sistema", "Pendiente", "Ruta sin configuracion especifica.", Severity.Info);

    private static Task Refresh() => Task.CompletedTask;

    private sealed record PendingMenuItem(
        string Title,
        string Subtitle,
        string Module,
        string Status,
        string Message,
        Severity Severity);
}
